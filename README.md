# thoughtmesh
ThoughtMesh is a Kubernetes-native platform for running and coordinating autonomous agents. This monorepo contains the core building blocks: the operator, custom resource definitions (CRDs), and the tm-agent runtime. Together, they provide a unified system for defining agent objectives, managing shared memory, and executing agentic loops at scale.
