---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: agents.core.thoughtmesh.dev
spec:
  group: core.thoughtmesh.dev
  names:
    kind: Agent
    listKind: AgentList
    plural: agents
    singular: agent
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Agent is the Schema for the agents API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Agent
            properties:
              endingCondition:
                description: EndingCondition defines when the agent should stop
                properties:
                  maxTurns:
                    description: MaxTurns is the maximum number of turns before the
                      agent stops
                    format: int32
                    minimum: 1
                    type: integer
                  natural:
                    description: Natural is an LLM-evaluated natural language stopping
                      condition
                    type: string
                  timeoutSeconds:
                    description: TimeoutSeconds is the maximum time in seconds before
                      the agent stops
                    format: int32
                    minimum: 1
                    type: integer
                type: object
              objective:
                description: Objective is the high-level goal of the agent
                type: string
            required:
            - objective
            type: object
          status:
            description: status defines the observed state of Agent
            properties:
              phase:
                description: Phase is the current lifecycle phase of the agent
                enum:
                - Pending
                - Running
                - Paused
                - Succeeded
                - Failed
                type: string
              startTime:
                description: StartTime is when the agent started running
                format: date-time
                type: string
              terminationReason:
                description: TerminationReason explains why the agent stopped
                enum:
                - NaturalConditionMet
                - MaxTurnsReached
                - TimedOut
                - Error
                - Stopped
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
